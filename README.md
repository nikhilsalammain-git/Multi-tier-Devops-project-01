# multi-tier-project
A containerized Flask application deployed on Amazon EKS, with infrastructure fully provisioned as code and a CI/CD pipeline for both infrastructure and application deployments.S rather than running its own database inside the cluster.
